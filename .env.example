# Database
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=banking_dashboard

# TrueLayer (Sandbox)
TRUELAYER_CLIENT_ID=your_sandbox_client_id
TRUELAYER_CLIENT_SECRET=your_sandbox_client_secret
TRUELAYER_REDIRECT_URI=http://localhost:8000/auth/callback
TRUELAYER_AUTH_URL=https://auth.truelayer-sandbox.com
TRUELAYER_API_URL=https://api.truelayer-sandbox.com

# Security
# Generate one with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=change_this_to_a_valid_fernet_key
